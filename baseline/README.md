# Dependencies

This requires libBGPStream, pkg-config, and cmake (`pkg-config`, `bgpstream`, and `libbgpstream-dev` on Ubuntu and `bgpstream` and `pkg-config` in brew).

# Building

To build, execute the following:

```
mkdir build && cd build
cmake ..
make -j
```
